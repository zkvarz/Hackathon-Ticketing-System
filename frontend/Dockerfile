# Placeholder — replaced by the real Vite build + nginx serving in HTS-003 (frontend baseline).
# Exists now so `docker compose config` validates the full stack and the build context resolves.
FROM nginx:alpine
# In HTS-003: copy the built SPA (dist/) and nginx.conf into the image.
