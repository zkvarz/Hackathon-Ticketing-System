# Placeholder — replaced by the real multi-stage Maven build in HTS-002 (Spring Boot baseline).
# Exists now so `docker compose config` validates the full stack and the build context resolves.
FROM eclipse-temurin:21-jre
WORKDIR /app
CMD ["sh", "-c", "echo 'backend placeholder — implement in HTS-002'; sleep infinity"]
